// Pedir para o usuário digitar 2 números e mostra a soma entre eles
const primeiroNumero = parseFloat(prompt("Digite o primeiro número: "));
const segundoNumero = parseFloat(prompt("Digite o segundo número: "));

alert(
  `A soma entre os 2 números digitados é: ${primeiroNumero + segundoNumero}`
);
